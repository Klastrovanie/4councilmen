# ── Infrastructure Settings ──
# INTERNAL_LLM_BASE_URL=http://host.docker.internal:11434/v1  (Mac/Windows Docker)
# INTERNAL_LLM_BASE_URL=http://172.17.0.1:11434/v1            (Linux Docker)
# INTERNAL_LLM_BASE_URL=http://localhost:11434/v1              (non-Docker)
# INTERNAL_LLM_MODEL=qwen2.5:14b

# ── API Keys (memory-only in v2.0.4+, optional fallback) ──
# ANTHROPIC_API_KEY=
# XAI_API_KEY=

# ── Change the name of the file to ".env" (without quotes) before using this──